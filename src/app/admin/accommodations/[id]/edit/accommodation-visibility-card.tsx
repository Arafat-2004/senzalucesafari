'use client'
import{useTransition}from'react';import{useRouter}from'next/navigation';import type{Accommodation}from'@/generated/prisma/client';import{setAccommodationActive}from'../../actions';import{Card,CardContent}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{toast}from'sonner';export function AccommodationVisibilityCard({item}:{item:Accommodation}){const[pending,start]=useTransition();const router=useRouter();return <Card><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><Badge variant={item.isActive?'default':'outline'}>{item.isActive?'Published':'Draft'}</Badge><p className="mt-2 text-sm text-muted-foreground">Public visibility is separate from editing property details.</p></div><Button variant={item.isActive?'outline':'default'} disabled={pending} onClick={()=>start(async()=>{try{await setAccommodationActive(item.id,!item.isActive);toast.success(item.isActive?'Returned to draft':'Accommodation published');router.refresh()}catch(e){toast.error(e instanceof Error?e.message:'Unable to update')}})}>{pending?'Updating…':item.isActive?'Return to draft':'Publish accommodation'}</Button></CardContent></Card>}
