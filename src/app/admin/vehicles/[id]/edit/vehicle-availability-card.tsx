'use client'
import{useTransition}from'react';import{useRouter}from'next/navigation';import type{Vehicle}from'@/generated/prisma/client';import{setVehicleActive}from'../../actions';import{Card,CardContent}from'@/components/ui/card';import{Button}from'@/components/ui/button';import{Badge}from'@/components/ui/badge';import{toast}from'sonner';export function VehicleAvailabilityCard({vehicle}:{vehicle:Vehicle}){const[pending,start]=useTransition();const router=useRouter();return <Card><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div><Badge variant={vehicle.isActive?'default':'outline'}>{vehicle.isActive?'Available':'Unavailable'}</Badge><p className="mt-2 text-sm text-muted-foreground">Availability controls whether this vehicle can be selected for new transfer requests.</p></div><Button variant={vehicle.isActive?'outline':'default'} disabled={pending} onClick={()=>start(async()=>{try{await setVehicleActive(vehicle.id,!vehicle.isActive);toast.success(vehicle.isActive?'Vehicle marked unavailable':'Vehicle made available');router.refresh()}catch(e){toast.error(e instanceof Error?e.message:'Unable to update')}})}>{pending?'Updating…':vehicle.isActive?'Mark unavailable':'Make available'}</Button></CardContent></Card>}
